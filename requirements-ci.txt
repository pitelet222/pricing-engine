# CI base dependencies — test, imports, openapi, and type-check jobs.
# Regenerate the lock file after any change:
#   make lock
# or:
#   uv pip compile requirements-ci.txt --python-platform linux --python-version 3.11 -o requirements-ci.lock
fastapi>=0.109
pydantic>=2.5
pydantic-settings>=2.2
prometheus-client>=0.20
redis>=5.0
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
plotly>=5.18
joblib>=1.3
starlette>=0.27
mypy>=1.8
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
fakeredis>=2.20
