# Integration test dependencies — toy-DataStore tests that build model artifacts.
# Regenerate the lock file after any change:
#   make lock
# or:
#   uv pip compile requirements-ci-integration.txt --python-platform linux --python-version 3.11 -o requirements-ci-integration.lock
fastapi>=0.109
pydantic>=2.5
pydantic-settings>=2.2
prometheus-client>=0.20
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
joblib>=1.3
scikit-learn>=1.3
lightgbm>=4.1
pytest>=8.0
httpx>=0.27
